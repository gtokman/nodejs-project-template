# Node JS Project Template

### Installation
* Clone repo
* `yarn install`
* `yarn dev` to run dev env

### Dependencies
* Puppeteer for UI testing
* Node-Fetch API client
* Express
* Dotenv
* Body parser
